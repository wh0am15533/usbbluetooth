[build-system]
requires = ["setuptools>=64", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "usbbluetooth"
dynamic = ["version"]
dependencies = []

[tool.setuptools_scm]
root = "../../../"
